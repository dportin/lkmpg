learning exercises following the linux kernel module programming guide

bear -- make to generate compile commands in a subdirectory

make all BEAR=1 to generate compile commands for all subdirectories
